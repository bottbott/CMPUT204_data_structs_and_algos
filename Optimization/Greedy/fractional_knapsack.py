from ast import List

def FractionalDecisions(items: List[(any, any)], max_weight):
    # the incoming items should have a weight and quantity.
    # the xi and vi amounts can be calculated at the time within the loop
    contents = []
    current_weight = 0
    while current_weight < max_weight:
        # extract highest value from list
        key = method()
        contents.append(items[key])
        items[key] = (0,0)
        current_weight = current_weight - 
    pass
